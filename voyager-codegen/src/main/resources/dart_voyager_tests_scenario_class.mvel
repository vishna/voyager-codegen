@experimental
class @{scenarioClassName} extends @{baseScenarioClassName} {
  @{scenarioClassName}.write(@foreach{param : params()}this.@{param},@end{} WidgetTesterCallback widgetTesterCallback, {String description = "", dynamic argument, bool stateless = false}) : super(description, widgetTesterCallback, argument: argument, stateless: stateless);

  @foreach{param : params()}final String @{param};@end{'\n'}

  @override
  String path() {
    return @{resolver.testPathExpression(routerPath)};
  }
}
