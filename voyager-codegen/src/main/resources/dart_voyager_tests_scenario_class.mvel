@experimental
class @{scenarioClassName} extends @{baseScenarioClassName} {
  @{scenarioClassName}.write(@foreach{param : params()}this.@{param},@end{} WidgetTesterCallback widgetTesterCallback, {String description = "", dynamic argument}) : super(description, widgetTesterCallback, argument: argument);

  @foreach{param : params()}final String @{param};@end{'\n'}

  @override
  String path() {
    return @{resolver.testPathExpression(routerPath)};
  }
}
